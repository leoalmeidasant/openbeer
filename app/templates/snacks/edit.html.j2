{% extends "_base.html.j2" %}
{% block base %}
  {{ super() }}
{% endblock base %}

{% block body %}
{% if message %}
<div class="ui message success">
  <i class="close icon"></i>
  <div class="header">
    Salvo com sucesso!
  </div>
  <p>{{message}}</p>
</div>
{% endif %}
<form class="ui form" action="{{ url_for('edit_snack', id=snack.id) }}" method="post" enctype="multipart/form-data">
<h4 class="ui dividing header">Dados da cerveja</h4>
<div class="two fields">
  <div class="field">
    <label for="name">Nome</label>
    <input type="text" name="name" placeholder="Nome" required="true" value="{{snack.name}}">
  </div>
  <div class="field">
    <label for="description">Descrição</label>
    <input type="text" name="description" placeholder="Descrição" required="true" value="{{snack.description}}">
  </div>
</div>
<div class="three fields">
  <div class="field">
    <label for="value">Valor</label>
    <input type="number" name="value" min="0" required="true" placeholder="R$" value="{{snack.value}}" step="0.1">
  </div>
  <div class="field">
    <label for="type">Tipo</label>
    <select class="ui fluid dropdown" name="type" value="{{snack.type}}">
      <option value="Pilsen">Pilsen</option>
      <option value="Pale Ale">Pale Ale</option>
      <option value="Lagger">Lagger</option>
    </select>
  </div>
  <div class="field">
    <label for="quantity">Quantidade (Garrafas Longneck)</label>
    <input type="number" name="quantity" placeholder="Ex: 10" min="0" required="true" value="{{snack.quantity}}">
  </div>
</div>
<div class="field">
  <label for="image">Imagem</label>
  <!-- <input type="file" name="image" accept="image/*" value="{{snack.image}}"> -->
  <img src="/static/images/{{snack.image}}" alt="">
</div>
<button type="submit" name="button" class="ui primary button">Salvar</button>
<a href="/snack" class="ui button">Cancelar</a>
</form>



<script type="text/javascript">
  $('select.dropdown')
  .dropdown()
  ;
  $('.message .close')
  .on('click', function() {
    $(this)
      .closest('.message')
      .transition('fade')
    ;
  })
;
</script>
{% endblock body %}
